mutation CreateCalendar($data: CalendarCreateInput!) {
	createCalendar(data: $data) {
		id
		track
		round
		deleted
		description
		date
		active
		link
		winnerA {
			name
		}
		winnerB {
			name
		}
		flag {
			id
			url
		}
		createdAt
	}
}

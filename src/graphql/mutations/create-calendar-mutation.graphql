mutation CreateCalendar($data: CalendarCreateInput!) {
	createCalendar(data: $data) {
		id
		trackId
		round
		sprint
		grid
		deleted
		active
		date
		link
		winnerA {
			id
			name
		}
		winnerB {
			id
			name
		}
		createdAt
	}
}

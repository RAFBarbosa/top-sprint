mutation CreateDriver($data: DriverCreateInput!) {
	createDriver(data: $data) {
		id
		name
		number
		grid
		class
		stream
		deleted
		city
		equipment
		phone
		photo {
			url
		}
		team {
			id
			name
			color {
				hex
			}
			photo {
				url
			}
		}
	}
}

mutation CreateDriver($data: DriverCreateInput!) {
	createDriver(data: $data) {
		id
		name
		number
		grid
		stream
		city
		equipment
		phone
		photo {
			url
		}
		team {
			id
			name
			color {
				hex
			}
			photo {
				url
			}
		}
	}
}

mutation CreateDriver($data: DriverCreateInput!) {
	createDriver(data: $data) {
		id
		deleted
		name
		number
		grid
		stream
		deleted
		city
		equipment
		phone
		badgeTitle
		badge {
			url
		}
		photo {
			url
		}
		team {
			id
			name
			color {
				hex
			}
			photo {
				url
			}
		}
	}
}

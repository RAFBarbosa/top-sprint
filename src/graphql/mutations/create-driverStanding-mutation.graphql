mutation CreateDriverStanding(
	$season: String!
	$round: String!
	$photo: AssetCreateOneInlineInput!
) {
	createDriverStanding(
		data: { season: $season, round: $round, photo: $photo }
	) {
		id
	}
}

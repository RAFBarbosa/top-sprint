mutation CreateHallOfFame($data: HallOfFameCreateInput!) {
	createHallOfFame(data: $data) {
		id
		name
		number
		grid
		stream
		city
		equipment
		phone
		photo {
			url
		}
		team {
			id
			name
			color {
				hex
			}
			photo {
				url
			}
		}
	}
}

query GetBanners {
	banners(orderBy: createdAt_DESC) {
		id
		title
		content
		link
		category
		photo {
			id
			url
		}
		createdAt
		updatedAt
	}
}

query GetBannersCategories {
	__type(name: "NewsCategory") {
		enumValues {
			name
		}
	}
}

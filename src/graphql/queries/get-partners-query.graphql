query GetPartners {
	partners(
		where: { active: true, deleted: false }
		orderBy: publishedAt_ASC
	) {
		id
		name
		altText
		deleted
		image {
			url
		}
		footerLogo {
			url
		}
		link
		active
	}
}

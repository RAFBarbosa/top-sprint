query GetTeams {
	drivers(stage: DRAFT, where: { deleted: false }) {
		id
		name
		number
		stream
		deleted
		city
		equipment
		grid
		class
		team {
			name
			color {
				hex
			}
			photo {
				url
			}
		}
		photo {
			url
		}
		badge {
			url
		}
		badgeTitle
	}
	teams(stage: DRAFT, where: { deleted: false }) {
		id
		name
		class
		deleted
		color {
			hex
		}
		photo {
			url
		}
	}
}

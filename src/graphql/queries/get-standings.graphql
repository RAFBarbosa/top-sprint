query GetTeams {
	drivers(stage: PUBLISHED, where: { fullTime: true }) {
		id
		name
		number
		stream
		city
		equipment
		grid
		team {
			name
			color {
				hex
			}
		}
		photo {
			url
		}
	}
	teams(stage: PUBLISHED) {
		color {
			hex
		}
		name
		id
		photo {
			url
		}
	}
}
